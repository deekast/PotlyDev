import { View, Text } from 'react-native';

export default function Pot() {
    return (
        <View className = 'flex flex-1 items-center justify-center'>
            <Text className='text-3xl font-bold text-foreground'>Pot Page</Text>
        </View>
    )
}